function [ set2_reordered ] = nearest_neighbor( set1, set2 )
%NEAREST_NEIGHBOR Summary of this function goes here
%   set1 is a collection of row vectors
%       NxM : N = Num Rows, M = columns
%   set2 is a collection of row vecttors
%       NxM : N = Num Rows, M = columns
% Returns the second set, reordered to match the first set. 
    

    for i=1:size()
        [
    end

end

