function [best_delta] = recover_pose_fun(fh, previous_pose, ...
                                previous_measurements, new_measurements)
% 
%  previous_pose - 6x1 of the previous pose.
%  best_delta - 6x1 
    if(fh ~= '')
        % TODO plot previous pose? or should someone else do this?
    end


end